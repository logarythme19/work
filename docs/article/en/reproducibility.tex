\section*{Reproducibility}
The code, the frozen protocol (\texttt{protocol/protocol\_v6\_2.json},
hash \ChiffreEmpreinte), the per-cell log, the seeds and the log of
deviations from the brief (\texttt{docs/ECARTS.md}) are published with the
article. Protocol v6.2 is chained to versions v6.1 and v6; every amendment is
justified. Every number in this article is generated by
\texttt{scripts/make\_numbers.py} from the JSON results.
